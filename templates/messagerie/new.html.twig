{% extends 'base.html.twig' %}

{% block title %}New Messagerie{% endblock %}

{% block body %}
    <h1>Create a new Messagerie</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit">Create</button>
    {{ form_end(form) }}

    <a href="{{ path('app_messagerie_index') }}">Back to list</a>
{% endblock %}

