<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&display=swap">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">

    <style>
        body {
            font-family: "Poppins", sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            display: flex;
            align-items: center;
            justify-content: center;
            height: 100vh;
        }

        .card {
            width: 500px;
            padding: 30px;
            border-radius: 12px;
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.1);
            text-align: center;
            color: #444;
            background-color: #fff;
            transition: 0.3s;
        }

        .card:hover {
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
        }

        .card img {
            width: 150px;
            height: 150px;
            object-fit: cover;
            border-radius: 50%;
            margin: 0 auto 20px; /* Center the image */
            display: block;
        }

        .input-group {
            display: flex;
            justify-content: space-between;
            margin-bottom: 20px;
        }

        .input-half {
            flex: 1;
            margin-right: 10px;
        }

        input {
            width: 100%;
            padding: 12px;
            font-size: 1rem;
            border: 1px solid #ddd;
            border-radius: 11px;
            box-sizing: border-box;
            color: #555;
            background-color: #f8f8f8;
            transition: 0.3s;
            margin-bottom: 10px; /* Add margin to the bottom for spacing */
        }

        .btn {
            width: 90%;
            height: 100%;
            background-color: #007bff;
            color: #fff;
            padding: 15px 20px;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            transition: 0.3s;
        }

        .btn:hover {
            background-color: #0056b3;
        }

        a {
            text-decoration: none;
            color: #007bff;
        }

        a:hover {
            color: #0056b3;
        }

        .alert {
            display: none;
            margin-bottom: 20px;
        }

        .alert-danger {
            color: #721c24;
            background-color: #f8d7da;
            border-color: #f5c6cb;
        }
        .custom-select {
            width: 100%;
            padding: 12px;
            font-size: 1rem;
            border: 1px solid #ddd;
            border-radius: 11px;
            box-sizing: border-box;
            color: #555;
            background-color: #f8f8f8;
            transition: 0.3s;
            margin-bottom: 10px;
            -webkit-appearance: none;
            -moz-appearance: none;
            appearance: none;
        }

        .custom-select:focus {
            border-color: #007bff;
            box-shadow: 0 0 10px rgba(0, 123, 255, 0.2);
        }

        .custom-select::after {
            content: "\25BC";
            position: absolute;
            top: 50%;
            right: 10px;
            transform: translateY(-50%);
            pointer-events: none;
            color: #555;
        }

        .custom-label {
            font-weight: bold;
            margin-bottom: 5px;
            display: block;
        }

    </style>
    <title>Formulare de Connexion</title>
</head>
<body>

<div class="card">
    <img src="{{ asset('img/logo.jpg') }}" alt="Gym Image">
    <h2>Inscription</h2>
    <div class="alert alert-danger" id="alertEmpty" role="alert">
        Veuillez remplir tous les champs.
    </div>

    <form method="post" action="{{ path('app_register') }}">
        <div class="input-group">
            <div class="input-half">
                {{ form_row(registrationForm.nom, {'attr': {'class': 'input-half', 'placeholder': 'nom'}}) }}            </div>
            <div class="input-half">
                <input type="text" id="prenom" name="prenom" placeholder="Prénom" required>
            </div>
        </div>
        <div class="input-group">
            <div class="input-half">
                <input type="email" id="email" name="email" placeholder="Email" required>
            </div>
            <div class="input-half">
                <input type="password" id="plainPassword" name="plainPassword" placeholder="Password" required>
            </div>
        </div>
        <div class="input-group">
            <div class="input-half">
                <input type="text" id="cin" name="cin" placeholder="Cin" required>
            </div>
            <div class="input-half">
                <input type="date" id="date_naissance" name="date_naissance" placeholder="Date De Naissance" required>
            </div>
        </div>

        <div class="input-group custom-dropdown" id="roles">
            <label for="{{ registrationForm.roles.vars.id }}" class="custom-label">Roles</label>
            {{ form_widget(registrationForm.roles, {'attr': {'class': 'custom-select' }}) }}
            {{ form_errors(registrationForm.roles) }}
        </div>



        <button class="btn" type="submit">Inscription</button>
    </form>
    <br>

    <p><a href="#">S'inscrire</a></p>
</div>

<script>
    function register() {
        var nom = document.getElementById("nom").value;
        var prenom = document.getElementById("prenom").value;
        var email = document.getElementById("email").value;
        var password = document.getElementById("password").value;
        var cin = document.getElementById("cin").value;
        var dateNaissance = document.getElementById("dateNaissance").value;
        var userType = document.getElementById("userType").value;

        // Check if any field is empty
        if (!nom || !prenom || !email || !password || !cin || !dateNaissance || !userType) {
            document.getElementById("alertEmpty").style.display = "block";
            return;
        }

        // Add your registration logic here
        // Example: Collect and validate the form data

        alert("Inscription réussie!");
        // Additional logic can be added here
    }
</script>
</body>
</html>
